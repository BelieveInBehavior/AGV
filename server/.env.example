# 复制为同目录下的 `server/.env`（真实文件已在 .gitignore，勿提交）。
# Docker Compose 的 `api` / `worker` 会加载：根目录 `.env` → 本文件（后者覆盖同名键）。
#
# 与 compose 内置 Mongo 联调：Docker 内 api/worker 用 mongo 主机名；宿主机 npm run dev 用 27018 映射
# MONGODB_URI=mongodb://mongo:27017
# MONGODB_DB_NAME=agv
# 宿主机直连同一实例：MONGODB_URI=mongodb://127.0.0.1:27018
#
# 远程 / 正式业务库（示例，请换成你的连接串与库名）：
# MONGODB_URI=mongodb://USER:PASS@HOST:27017
# MONGODB_DB_NAME=beeseen
#
