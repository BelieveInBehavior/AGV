# 勿设置 VITE_API_ORIGIN：开发时用相对路径 /api，由 Vite 代理到 CWEI_PORT（避免 CORS）
# 需要 Network 里显示后端端口时，在 .env.development.local 里单独设置（并确保 API 已启用 CORS）
CWEI_PORT=3001
# 开发环境跳过登录页，自动用测试账号登录（与 server/.env 中 TEST_PHONE_* 一致）
VITE_DEV_AUTO_LOGIN=1
